{
  "buildCommand": "npm install --cwd ssv-react && npm run build --cwd ssv-react",
  "outputDirectory": "ssv-react/dist",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
