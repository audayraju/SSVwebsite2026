{
  "buildCommand": "cd ssv-react && npm ci && npm run build",
  "outputDirectory": "ssv-react/dist",
  "framework": "vite",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
